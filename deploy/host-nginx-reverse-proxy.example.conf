# Пример для ХОСТОВОГО nginx (не Docker), когда порты 80/443 уже заняты,
# а CRM крутится в Docker с пробросом nginx как 8888->80 (и при необходимости 8443->443).
#
# Куда положить: зависит от ОС (часто /etc/nginx/conf.d/crm.master15.ru.conf или
# фрагмент в sites-enabled). После правок: nginx -t && systemctl reload nginx
#
# DNS: crm.master15.ru → IP этого сервера (у вас уже так).
#
# ВАЖНО (частая причина 403 только из интернета):
# На VPS панель часто добавляет отдельный socket: listen 45.x.x.x:80 default_server;
# Запросы на публичный IP тогда НЕ попадают в блок с listen 80 (*:80), а в «дефолтный»
# vhost панели → 403. С localhost (127.0.0.1) при этом всё работает.
# Решение: в этом же server {} добавьте строку listen с ВАШИМ публичным IP, например:
#   listen 45.128.204.68:80;
# Если nginx ругается на «duplicate listen» — уберите конфликтующий default_server
# у панели или объедините конфиги (смотрите: nginx -T | grep listen).

# Нужно для корректной передачи WebSocket на /ws/
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# --- HTTP ---
# Порядок работы:
# 1) Сначала включите только блок ниже с proxy_pass (и БЕЗ return 301), выпустите сертификат.
# 2) Потом добавьте server { listen 443 ssl ... } и включите return 301 на HTTPS в блоке :80.
server {
    listen 80;
    listen [::]:80;
    # Раскомментируйте и подставьте IP VPS, если снаружи 403, а curl -H Host с 127.0.0.1 даёт 200:
    # listen 45.128.204.68:80;
    server_name crm.master15.ru;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://127.0.0.1:8888;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    # После настройки HTTPS на хосте — раскомментируйте редирект и уберите дублирующий location / выше
    # (оставьте только acme-challenge + return):
    # return 301 https://$host$request_uri;
}

# --- HTTPS (раскомментируйте целиком после выпуска сертификата на хосте) ---
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name crm.master15.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/crm.master15.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/crm.master15.ru/privkey.pem;
#
#     client_max_body_size 25m;
#
#     location / {
#         proxy_pass http://127.0.0.1:8888;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 300s;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection $connection_upgrade;
#     }
# }
